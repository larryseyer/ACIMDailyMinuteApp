import SwiftUI

struct ArchiveSearchView: View {
    var body: some View {
        ContentUnavailableView(
            "Search coming in Phase 3.7",
            systemImage: "magnifyingglass"
        )
    }
}
