# Ralph Progress Log — ACIM Daily Minute Phases 3.9 → 5 (Finish the App)
Started: 2026-04-14
Branch: ralph/acim-3.9-to-5-finish-2026-04-14 (cut off main; not yet created at time of PRD authoring)
Mission: Land the entire remaining roadmap — Widget target (3.9), Watch companion (3.10), feature-parity sweep (4), and branding + ACIMChime.caf wiring + App Store metadata (5) — so the app is feature-complete and ready for App Store submission.
---

## Seed context (read before first iteration)

Phase 3.8 (Saved + Settings + Onboarding + Deep Links + Shortcut) shipped at commit **814731e on `ralph/acim-3.8-finish-2026-04-14`**. All 5 main app tabs render real content. No TODOs in code. Main iOS target compiles clean. The Widget and Watch targets are still broken-by-design against the post-3.2 SwiftData schema — this is the entire point of stories 3.9a and 3.10a.

CONTINUE.md was deleted as part of this PRD pair's authoring (per Larry's direction on 2026-04-14). It was a stale summary that overstated what was built. The authoritative source for remaining scope is THIS prd.json + this progress.txt + git history.

Thirteen stories remain (one Ralph iteration each):

| ID | Chunk | One-line |
|---|---|---|
| ACIM-001 | 3.9a | Widget schema parity — bring Widget target compile-clean against current SwiftData schema |
| ACIM-002 | 3.9b | Small widget family (today's minute excerpt + date) |
| ACIM-003 | 3.9c | Medium widget family (minute + lesson + CTA) |
| ACIM-004 | 3.9d | Large widget family (full text + bookmark indicator) |
| ACIM-005 | 3.9e | Live Activity UI (ActivityAttributes + Dynamic Island layouts) |
| ACIM-006 | 3.10a | Watch schema parity + Today view |
| ACIM-007 | 3.10b | Watch complications × 3 (circular, rectangular, inline) |
| ACIM-008 | 3.10c | Watch/phone WCSession one-way sync (phone → watch latest minute) |
| ACIM-009 | 4.0 | Feature parity verification + PARITY.md artifact |
| ACIM-010 | 5a | App icon set (1024×1024 master + sizes) |
| ACIM-011 | 5b | Launch screen + accent color polish |
| ACIM-012 | 5c | ACIMChime.caf custom notification sound wiring |
| ACIM-013 | 5d | App Store metadata dry-run (APP_STORE_LISTING.md) |

After ACIM-013 passes AND ./build.sh exits 0: emit `<promise>COMPLETE</promise>` so run_ralph.sh exits.

## Pre-flight (Larry's job before iteration 1)

- [ ] Cut branch `ralph/acim-3.9-to-5-finish-2026-04-14` off `main`.
- [ ] Decide branch-push strategy. `bu.sh` line `git push origin main` is hard-coded. Either:
  - (a) Edit `bu.sh` to push to the new branch for the duration of this PRD pair, then revert at end, OR
  - (b) Have Ralph run raw `git commit` + `git push origin HEAD` instead of `./bu.sh` (loses the Dropbox zip backup), OR
  - (c) Modify `bu.sh` to detect current branch and push to it (preferred long-term — one-line change: `git push origin "$(git symbolic-ref --short HEAD)"`).
- [ ] Confirm Apple Developer team / signing config still works for Widget + Watch + Live Activity entitlements (these targets haven't built since Phase 3.2; signing may need re-up).
- [x] Phase 5a master icon: RESOLVED — complete Apple iconset pre-staged at `/Users/larryseyer/ACIMDailyMinuteApp/assets/AppIcon.appiconset/` including 1024×1024 master (`icon_512x512@2x.png`). Story 5a installs from here.

## Execution model (hard rules for every iteration)

- Working directory: `/Users/larryseyer/ACIMDailyMinuteApp/`
- Build target: iPad (10th generation) iOS 18.1, sim UUID `86F64729-D28D-44F7-BEB9-EF34AA7B7F28`. Watch target: Apple Watch Series 10 (46mm). `./build.sh` already resolves both correctly via xcrun simctl.
- **MUST run ./build.sh before each commit** — this is the verification command for ALL stories. Use the per-phase error filter that matches the story:
  ```
  # After 3.9a: Widget target must be 0
  grep "error:" /tmp/full-build.log | grep -E "(ACIMDailyMinuteWidget|WidgetExtension)" | wc -l
  # After 3.10a: Watch target must be 0
  grep "error:" /tmp/full-build.log | grep -E "ACIMDailyMinuteWatch" | wc -l
  # From 4.0 onward: full ./build.sh exit 0
  ```
- Commits via `./bu.sh "<message>"` (subject to the branch-push fix-up above). Format: `./bu.sh "<chunk-id> — <chunk title>"`. One commit per story.
- After commit succeeds, set `passes:true` on the story in prd.json AND append a completion block to this file under `## --- Iteration Log ---`.

## Verified facts (grep-confirmed against HEAD on 2026-04-14)

- **`ACIMChime.caf` exists** at `/Users/larryseyer/ACIMDailyMinuteApp/ACIMDailyMinute/Resources/ACIMChime.caf` and at `/Users/larryseyer/ACIMDailyMinuteApp/assets/ACIMChime.caf`. Story 5c does NOT create the file — only verifies pbxproj membership on the Resources/ copy and wires `NotificationManager.preferredSound()`.
- **`AppIcon.appiconset` and `AccentColor.colorset` directories exist** under `ACIMDailyMinute/Assets.xcassets/`. Stories 5a/5b populate them; do NOT create them.
- **Pre-staged Apple iconset exists** at `/Users/larryseyer/ACIMDailyMinuteApp/assets/AppIcon.appiconset/` (10 PNGs: 16/32/64/128/256/512/1024 incl. @2x retina) and at `assets/ACIM.iconset/` (iconutil format) plus `assets/ACIM.icns` (compiled macOS). 1024×1024 master is `icon_512x512@2x.png`. Story 5a installs from here — does NOT regenerate with sips. Icon-master blocker from the pre-flight is RESOLVED.
- **All Widget + Watch source files exist** — they're schema-stale, not absent. SmallWidgetView.swift / MediumWidgetView.swift / LargeWidgetView.swift / ACIMDailyMinuteLiveActivity.swift / ACIMDailyMinuteTimelineProvider.swift / WidgetStoryEntry.swift / SharedModelContainer.swift / ACIMDailyMinuteWidget.swift on the Widget side; ACIMDailyMinuteWatchApp.swift / WatchContentView.swift / WatchDataService.swift / WatchStoryRow.swift / ACIMDailyMinuteWatchWidget.swift on the Watch side.
- **Highest pbxproj fileRef in current use: AA000002222** (DeepLinkRoute, from Phase 3.8d). Next free range for this PRD: `AA000001225+ / AA000002225+`. Three new files anticipated need IDs: PhoneWatchSyncService.swift (3.10c → AA000001226 / AA000002226), ACIMChime.caf if not yet a member (5c → AA000001227 / AA000002227). ACIMDailyMinuteWatchWidget.swift if not yet a member (3.10b → AA000001225 / AA000002225). Asset catalog edits don't need new IDs.
- **`bu.sh` pushes to `origin main` hard-coded** — see Pre-flight item above. Story 1 must not run until this is resolved or the push will fail / land on the wrong branch.

## Existing patterns to reuse (do NOT re-derive)

- **App Group identifier:** `group.com.larryseyer.acimdailyminute`. Used by main app, Widget target, Watch target. Same for the WCSession identifier convention if needed.
- **Schema registration shape:** see `ACIMDailyMinute/App/ACIMDailyMinuteApp.swift` for the canonical `Schema([DailyMinute.self, DailyLesson.self, ArchivedReading.self, Bookmark.self])` + `ModelConfiguration(schema:, isStoredInMemoryOnly: false, groupContainer: .identifier("group.com.larryseyer.acimdailyminute"))`. Stories 3.9a and 3.10a must mirror this exactly.
- **Deep-link URL scheme:** `acimdailyminute://` already registered in Info.plist (Phase 3.8d). Widget `.widgetURL()` calls in 3.9b/c/d use this.
- **NavigationStack(path:) + .navigationDestination(for:):** Lessons (Int), Archive (String). Watch should follow the same pattern when 3.10a's Today view grows.
- **iOS / macOS / watchOS conditional UI:** `#if os(iOS) ... #elseif os(watchOS) ... #else ... #endif`. Story 3.10c's WCSessionDelegate has iOS-only delegate methods that must be `#if os(iOS)`-gated.
- **NotificationManager UNMutableNotificationContent construction:** existing call sites in scheduleDailyReminder + fireTest. Story 5c adds `preferredSound()` helper and replaces `.sound = .default` calls with it.

## Story dependency map

```
ACIM-001 (3.9a Widget schema parity)
  ├─ ACIM-002 (3.9b Small)
  ├─ ACIM-003 (3.9c Medium)
  ├─ ACIM-004 (3.9d Large)
  ├─ ACIM-005 (3.9e Live Activity)
  └─ ACIM-006 (3.10a Watch schema parity + Today)
       ├─ ACIM-007 (3.10b complications)
       └─ ACIM-008 (3.10c WCSession sync)

ACIM-009 (4.0 PARITY.md) requires ACIM-005 + ACIM-008 (whole 3.9 + 3.10 done)

ACIM-010 (5a icon)         ┐
ACIM-011 (5b launch+accent)│  all require ACIM-009
ACIM-012 (5c ACIMChime)    ┘

ACIM-013 (5d APP_STORE_LISTING.md) requires ACIM-010 + ACIM-011 + ACIM-012
```

Ralph picks the lowest-priority `passes:false` story whose `depends_on` are all `passes:true`. With this dependency graph, the linear order matches the priority order 1 → 13.

---
## --- Iteration Log ---

### ACIM-001 — 3.9a — Widget schema parity
- **Status:** PASS
- **Date:** 2026-04-14
- **Files changed:**
  - `ACIMDailyMinuteWidget/WidgetStoryEntry.swift` — replaced WidgetStory/stories array with flat entry: minuteText, lessonNumber, publishedAt
  - `ACIMDailyMinuteWidget/SharedModelContainer.swift` — replaced createReadOnly() with static shared ModelContainer singleton
  - `ACIMDailyMinuteWidget/ACIMDailyMinuteTimelineProvider.swift` — rewrote to fetch DailyMinute + DailyLesson via SwiftData instead of dead Story/Source types
  - `ACIMDailyMinuteWidget/SmallWidgetView.swift` — placeholder (3.9b builds real UI)
  - `ACIMDailyMinuteWidget/MediumWidgetView.swift` — placeholder (3.9c builds real UI)
  - `ACIMDailyMinuteWidget/LargeWidgetView.swift` — placeholder (3.9d builds real UI)
  - `ACIMDailyMinuteWidget/ACIMDailyMinuteLiveActivity.swift` — fixed to compile against real ACIMActivityAttributes.ContentState (3.9e builds real UI)
  - `ACIMDailyMinuteWidget/ACIMDailyMinuteWidget.swift` — description text updated
- **Build verification:** Widget errorFilter = 0 (grep 'error:' | grep Widget/WidgetExtension | wc -l = 0). Pre-existing SettingsView.swift iOS 18 availability error in main target — not widget-related, not touched.
- **Old schema names:** zero StoryEntry/FactItem/NewsItem/JTFStory/JTFNews references in Widget target (WidgetStoryEntry name retained per AC — grep false positive only).

### ACIM-002 — 3.9b — Small widget family (today's minute + date)
- **Status:** PASS
- **Date:** 2026-04-14
- **Files changed:**
  - `ACIMDailyMinuteWidget/SmallWidgetView.swift` — full layout: VStack with relative date string (Today/Yesterday/formatted), minute text (callout, 4-line limit), lesson number badge; .widgetURL deep link to acimdailyminute://today; .containerBackground(.fill.tertiary)
  - `ACIMDailyMinuteWidget/ACIMDailyMinuteWidget.swift` — removed outer .containerBackground(Color(white: 0.1)) from WidgetEntryView so per-view backgrounds take effect; .systemSmall routing and supportedFamilies already present from 3.9a
- **Build verification:** Widget errorFilter = 0. Watch errors still allowed (pre-3.10a).
- **Grep checks:** zero TODO/FIXME/XXX/HACK/JTFNews in touched files.

### ACIM-003 — 3.9c — Medium widget family (minute + lesson + CTA)
- **Status:** PASS
- **Date:** 2026-04-14
- **Files changed:**
  - `ACIMDailyMinuteWidget/MediumWidgetView.swift` — full HStack layout: left VStack with relative date + minute text (subheadline, 5-line limit); Divider; right VStack (width 100) with lesson label + number (title2, semibold) + "Open today's minute" CTA tint text; extracted relativeDateString as private free function; .widgetURL deep link to acimdailyminute://today; .containerBackground(.fill.tertiary)
- **Build verification:** Widget errorFilter = 0 (grep 'error:' | grep Widget/WidgetExtension | wc -l = 0). Watch errors still allowed (pre-3.10a).
- **Grep checks:** zero TODO/FIXME/XXX/HACK/JTFNews in touched files.

### ACIM-004 — 3.9d — Large widget family (full text + bookmark indicator)
- **Status:** PASS
- **Date:** 2026-04-14
- **Files changed:**
  - `ACIMDailyMinuteWidget/WidgetStoryEntry.swift` — added `isBookmarked: Bool` field (default false) to TimelineEntry; updated placeholder and empty statics
  - `ACIMDailyMinuteWidget/ACIMDailyMinuteTimelineProvider.swift` — added Bookmark fetch in fetchEntry(): FetchDescriptor<Bookmark> with #Predicate matching "minute:{segmentHash}" key, populates isBookmarked field
  - `ACIMDailyMinuteWidget/LargeWidgetView.swift` — full layout: VStack with HStack header (relative date + bookmark.fill icon), body text (12-line limit), lesson number footer with book.closed icon; relativeDateString helper; .widgetURL deep link to acimdailyminute://today; .containerBackground(.fill.tertiary)
  - `ACIMDailyMinuteWidget/ACIMDailyMinuteWidget.swift` — no changes needed (.systemLarge routing and supportedFamilies already present from 3.9a)
- **Build verification:** Widget errorFilter = 0 (grep 'error:' | grep Widget/WidgetExtension | wc -l = 0). Pre-existing SettingsView.swift iOS 18 availability error in main target — not widget-related, not touched. Watch errors still allowed (pre-3.10a).
- **Grep checks:** zero TODO/FIXME/XXX/HACK/JTFNews in touched files.

### ACIM-005 — 3.9e — Live Activity UI (ActivityAttributes + Dynamic Island)
- **Status:** PASS
- **Date:** 2026-04-14
- **Files changed:**
  - `ACIMDailyMinute/Models/ACIMActivityAttributes.swift` — renamed struct ACIMActivityAttributes → ACIMDailyMinuteAttributes; updated ContentState fields: removed channel, renamed latestText→minuteText, publishedDate→publishedAt; added static let name = "Daily Minute"
  - `ACIMDailyMinute/Services/LiveActivityManager.swift` — updated all references from ACIMActivityAttributes to ACIMDailyMinuteAttributes; removed channel from ContentState construction; updated field names in startOrUpdate, endAllActivities, scheduleDismissal
  - `ACIMDailyMinuteWidget/ACIMDailyMinuteLiveActivity.swift` — full rewrite: Lock Screen UI with VStack header (sun.max icon + title + lesson number) + minute text (4-line limit); Dynamic Island expanded with leading (sun.max + ACIM label), trailing (lesson number), empty center, bottom (minute text 3-line limit); compact leading (sun.max), compact trailing (lesson number), minimal (sun.max)
- **Build verification:** Widget errorFilter = 0 (grep 'error:' | grep Widget/WidgetExtension | wc -l = 0). Pre-existing SettingsView.swift iOS 18 availability error in main target — not widget-related, not touched. Watch errors still allowed (pre-3.10a).
- **Grep checks:** zero TODO/FIXME/XXX/HACK/JTFNews in touched files.

### ACIM-006 — 3.10a — Watch schema parity + Today view
- **Status:** PASS
- **Date:** 2026-04-14
- **Files changed:**
  - `ACIMDailyMinuteWatch/WatchDataService.swift` — converted from actor to final class singleton (.shared); creates own ModelContainer with same Schema/App Group as main app; added @MainActor fetchTodaysMinute() and fetchTodaysLessonNumber(); retained existing network fetch (fetchDailyContent) and DTOs
  - `ACIMDailyMinuteWatch/WatchContentView.swift` — full rewrite: removed old Story/@Query pattern; NavigationStack > List > Section("Today") with WatchStoryRow or "No content yet" empty state; @State minute/lessonNumber populated via .task from WatchDataService.shared
  - `ACIMDailyMinuteWatch/WatchStoryRow.swift` — full rewrite: takes DailyMinute + optional lessonNumber; VStack with lesson badge + minute text (6-line limit); no tap action per AC
  - `ACIMDailyMinuteWatch/ACIMDailyMinuteWatchApp.swift` — simplified: removed inline sharedModelContainer; uses .modelContainer(WatchDataService.shared.container)
  - `ACIMDailyMinuteWatch/ACIMDailyMinuteWatchWidget.swift` — cleaned old schema refs (WatchStoryEntry→WatchTimelineEntry, storyCount/latestFact→lessonNumber/minuteSnippet, Story→DailyMinute, SharedModelContainer.createReadOnly()→SharedModelContainer.shared); minimal update for grep compliance, full rewrite deferred to ACIM-007
- **Build verification:** Widget errorFilter = 0, Watch errorFilter = 0 (grep 'error:' | grep ACIMDailyMinuteWatch | wc -l = 0). watchOS standalone build exit=0. Pre-existing SettingsView.swift iOS 18 availability error in main target — not watch-related, not touched.
- **Grep checks:** zero TODO/FIXME/XXX/HACK/JTFNews in Watch target. Zero StoryEntry/FactItem/NewsItem/JTFStory in Watch target.
